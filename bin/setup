#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
#
# if [ -d "~/Dropbox" ]; then
#   if [ ! -e ~/.gif_manager.yml ]; then
#     echo "ENV_DIR_KEY: '~/Dropbox'" > ~/.gif_manager.yml
#   fi
# fi
